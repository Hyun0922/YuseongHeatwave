window.YCharts={bars(el,rows,key,label){const max=Math.max(...rows.map(r=>r[key]||0),1);el.innerHTML=rows.map(r=>`<div class="barrow"><span>${r[label]}</span><div class="bartrack"><div class="barfill" style="width:${(r[key]||0)/max*100}%"></div></div><b>${r[key]||0}</b></div>`).join('')},grades(el,dist){const colors=APP_CONFIG.gradeColors;const total=Object.values(dist).reduce((a,b)=>a+b,0);el.innerHTML=`<div class="stack">${[1,2,3,4,5].map(g=>`<span title="${g}등급 ${dist[g]||0}개" style="width:${(dist[g]||0)/total*100}%;background:${colors[g]}">${dist[g]||0}</span>`).join('')}</div><div class="note" style="display:flex;justify-content:space-between;margin-top:8px">${[1,2,3,4,5].map(g=>`<span>${g}등급</span>`).join('')}</div>`},matrix(el,m){let h='<table class="heatmatrix"><tr><th>거주\\활동</th>'+[1,2,3,4,5].map(x=>`<th>${x}</th>`).join('')+'</tr>';const max=Math.max(...m.flat());for(let i=0;i<5;i++){h+=`<tr><th>${i+1}</th>`;for(let j=0;j<5;j++){const v=m[i][j],a=.12+.78*v/max;h+=`<td style="background:rgba(106,0,168,${a}) ;color:${a>.5?'white':'#333'}">${v}</td>`}h+='</tr>'}el.innerHTML=h+'</table>'},scatter(el,features){const pts=features.map(f=>f.properties).filter(p=>p.score_res!=null&&p.score_act!=null);const W=700,H=330,pad=38;const xs=pts.map(p=>p.score_res),ys=pts.map(p=>p.score_act),xmin=Math.min(...xs),xmax=Math.max(...xs),ymin=Math.min(...ys),ymax=Math.max(...ys);const sx=x=>pad+(x-xmin)/(xmax-xmin)*(W-pad*2),sy=y=>H-pad-(y-ymin)/(ymax-ymin)*(H-pad*2);el.innerHTML=`<svg viewBox="0 0 ${W} ${H}" width="100%" height="100%"><line x1="${pad}" y1="${H-pad}" x2="${W-pad}" y2="${H-pad}" stroke="#999"/><line x1="${pad}" y1="${pad}" x2="${pad}" y2="${H-pad}" stroke="#999"/>${pts.map(p=>`<circle cx="${sx(p.score_res)}" cy="${sy(p.score_act)}" r="3" fill="${APP_CONFIG.diagnosisColors[p.diagnosis]}" opacity=".65"><title>${p.grid_id} · 거주 ${p.score_res.toFixed(1)} · 활동 ${p.score_act.toFixed(1)}</title></circle>`).join('')}<text x="${W/2}" y="${H-5}" text-anchor="middle" font-size="12">취약계층 밀집형 점수</text><text x="12" y="${H/2}" transform="rotate(-90 12 ${H/2})" text-anchor="middle" font-size="12">보행 노출 위험형 점수</text></svg>`}};